class Solution {
    public int maximumProduct(int[] arr) {
        int n = arr.length;
        Arrays.sort(arr);
        int product = arr[0] * arr[1] * arr[n - 1];
        int product1 = arr[n - 1] * arr[n - 2] * arr[n - 3];
        return Math.max(product,product1);
    }
}
